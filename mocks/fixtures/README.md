# Fixtures (synthetic)

All data here is **synthetic and de-identified** — safe to commit. Never add real PHI.

- `studycontext.sample.json` — a valid `StudyContext` envelope (validated in CI).
- `fhir_bundle.sample.json` — a small synthetic FHIR R4 bundle for EHR Assistant (M1) development.

Add more fixtures as agents grow; wire new ones into `scripts/validate_contracts.py`.
