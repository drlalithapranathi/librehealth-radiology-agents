# Dependencies for the MIMIC-CXR showcase ETL tooling (#68). Kept out of the repo's core deps:
# only the demo host that loads the cohort needs these.
pydicom>=2.4        # DICOM read/fix-up
boto3>=1.34         # selective pull from the PhysioNet S3 bucket (fetch tool)
httpx>=0.27         # fhir2 / OpenMRS REST writes
